apply plugin: 'java'

jar {

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

dependencies {
    compile group: 'org.apache.kafkaMirror', name: 'kafkaMirror-clients', version: '1.1.0'
    compile project(':common')

}